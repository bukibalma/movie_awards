Drop your IMDb CSV export(s) here. The app checks this folder every few
minutes and imports anything it finds automatically, then moves the file
into processed/ so it won't be imported twice.

Export from IMDb: sign in -> your profile -> Your Ratings (or any list) ->
the ... menu -> Export. That downloads a CSV — save/move it into this
folder.
